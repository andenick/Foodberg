# Carson pre-push secret-scan exemptions (PREPUSH_GITLEAKS_HOOK).
#
# One extended-regular-expression per line, matched case-insensitively against
# the scanner's `path:line:content` rows. A rule here is a RECORDED, reviewable
# exemption — the whole point of this file is that a false positive is written
# down in the diff once, rather than waved through with SKIP_GITLEAKS on every
# push. Keep each rule as narrow as the evidence supports, and say why.
#
# `gitleaks detect` over the full history reports CLEAN. Everything below is the
# hook's secondary "hardcoded credential assignment" heuristic firing on code
# that READS a key from configuration rather than embedding one.

# Technical/server/[2025.09.25] index.js — the retired Node prototype server.
# Three lines match, and all three read from a config object; none contains a
# literal secret:
#   :31   const fdcApiKey = apiKeysConfig.primary_sources?.usda_fdc_api_key;
#   :379  const hasApiKey = sourceConfig.api_key_required ? ... : true;
#   :425  const hasApiKey = sourceConfig.api_key_required ? ... : true;
# The adjacent marsApiKey line compares against the literal placeholder
# 'YOUR_MARS_API_KEY_HERE', which is the shape the hook already treats as benign.
# The real MARS key lives in the OS credential vault and is read at runtime via
# keyring.get_password(...) — see DEPLOYMENT_TRUTH.md §7. This file is a dead
# configuration (§6) retained for history and is not deployed.
^Technical/server/\[2025\.09\.25\] index\.js:(31|379|425):
